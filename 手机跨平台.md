# ReadStorm 手机跨平台迁移技术方案

> **目标**：将现有 .NET 10 + Avalonia 桌面阅读器迁移至 Android / iOS 手机端，实现一套代码同时支持桌面与移动平台。

---

## 一、现状分析

### 1.1 当前架构

```
ReadStorm/
├── ReadStorm.Domain          # 领域模型（纯 C#，零依赖）
├── ReadStorm.Application     # 用例接口（仅依赖 Domain）
├── ReadStorm.Infrastructure  # 基础设施（AngleSharp、SQLite）
├── ReadStorm.Desktop         # Avalonia 桌面 UI + DI 入口
└── ReadStorm.Tests           # xUnit 测试
```

**依赖关系**：`Desktop → Infrastructure → Application → Domain`

### 1.2 技术栈盘点

| 组件 | 技术 | 移动端兼容性 |
|------|------|-------------|
| UI 框架 | Avalonia 11.3 | ✅ 原生支持 Android/iOS |
| MVVM | CommunityToolkit.Mvvm 8.2 | ✅ 完全兼容 |
| DI 容器 | Microsoft.Extensions.DI 10.0 | ✅ 完全兼容 |
| HTML 解析 | AngleSharp 1.1 | ✅ 完全兼容 |
| 数据库 | Microsoft.Data.Sqlite 9.0 | ✅ 支持（需配置路径） |
| 书源规则 | JSON 文件 | ✅ 兼容（需调整读取路径） |
| 主题 | Semi.Avalonia 11.2 | ⚠️ 部分兼容，需验证移动端表现 |
| 构建目标 | net10.0 | ✅ 支持 `net10.0-android` / `net10.0-ios` |

### 1.3 可行性结论

**改造成本：中等偏低。** 原因：

1. **架构优势**：清洁架构四层分离，Domain / Application / Infrastructure 三层 **零 UI 耦合**，可 100% 复用。
2. **Avalonia 原生支持**：Avalonia 11 已内置 Android/iOS 平台支持，无需更换框架。
3. **依赖全兼容**：所有 NuGet 包均支持移动端运行时。
4. **主要工作量**：UI 适配（触控交互 + 屏幕尺寸）和平台特定配置。

---

## 二、技术路线选择

### 方案对比

| 方案 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **A. Avalonia 移动端** | 在现有 Avalonia 基础上添加 Android/iOS 目标 | 复用现有 XAML/ViewModel；学习成本最低 | Avalonia 移动端生态较新，部分控件需适配 |
| B. .NET MAUI | 用 MAUI 重写 UI 层 | 微软官方支持，社区更大 | 需重写全部 XAML；两套 UI 维护成本高 |
| C. React Native | 用 JS/TS 重写前端 | 移动端生态成熟 | 需放弃 .NET UI 层；跨语言通信复杂 |

### ✅ 推荐方案：A — Avalonia 移动端扩展

**核心理由**：
- 现有 ViewModels、XAML 样式、数据绑定 **可直接复用**，仅需针对移动端做响应式布局调整
- Avalonia 11 对 Android/iOS 的支持已进入稳定阶段
- 业务层（Domain + Application + Infrastructure）**零改动**
- 团队无需学习新框架

---

## 三、分步实施计划

### 📋 总览

| 阶段 | 内容 | 预估工时 | 依赖 |
|------|------|----------|------|
| 第一阶段 | 项目结构搭建 & 基础运行 | 2-3 天 | 无 |
| 第二阶段 | UI 响应式适配 | 5-7 天 | 阶段一 |
| 第三阶段 | 平台特定适配 | 3-5 天 | 阶段二 |
| 第四阶段 | 阅读器移动端优化 | 3-4 天 | 阶段二 |
| 第五阶段 | 测试 & 调试 | 3-5 天 | 阶段三、四 |
| 第六阶段 | 打包发布 | 2-3 天 | 阶段五 |

**总计：约 18-27 个工作日（3.5-5.5 周）**

---

### 阶段一：项目结构搭建 & 基础运行（2-3 天）

#### 1.1 新建移动端项目

在解决方案中添加两个新项目，复用现有的 Application / Infrastructure / Domain：

```
src/
├── ReadStorm.Android/          # 新增：Android 启动项目
│   ├── ReadStorm.Android.csproj
│   ├── MainActivity.cs
│   ├── AndroidManifest.xml
│   └── Resources/
├── ReadStorm.iOS/              # 新增：iOS 启动项目
│   ├── ReadStorm.iOS.csproj
│   ├── AppDelegate.cs
│   ├── Info.plist
│   └── Assets.xcassets/
└── ReadStorm.Desktop/          # 现有：桌面项目（不动）
```

#### 1.2 Android 项目配置

```xml
<!-- ReadStorm.Android.csproj -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0-android</TargetFramework>
    <OutputType>Exe</OutputType>
    <ApplicationId>com.readstorm.app</ApplicationId>
    <ApplicationVersion>1</ApplicationVersion>
    <ApplicationDisplayVersion>1.0</ApplicationDisplayVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="11.3.11" />
    <PackageReference Include="Avalonia.Android" Version="11.3.11" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="11.3.11" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="11.3.11" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.2.1" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.0" />
    <PackageReference Include="Semi.Avalonia" Version="11.2.1" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ReadStorm.Application\ReadStorm.Application.csproj" />
    <ProjectReference Include="..\ReadStorm.Infrastructure\ReadStorm.Infrastructure.csproj" />
  </ItemGroup>
</Project>
```

#### 1.3 Android 入口文件

```csharp
// MainActivity.cs
using Android.Content.PM;
using Avalonia;
using Avalonia.Android;

namespace ReadStorm.Android;

[Activity(
    Label = "ReadStorm",
    Theme = "@style/MyTheme.NoActionBar",
    Icon = "@drawable/icon",
    MainLauncher = true,
    ConfigurationChanges = ConfigChanges.Orientation
        | ConfigChanges.ScreenSize
        | ConfigChanges.UiMode)]
public class MainActivity : AvaloniaMainActivity<App>
{
    protected override AppBuilder CustomizeAppBuilder(AppBuilder builder)
    {
        return base.CustomizeAppBuilder(builder)
            .WithInterFont()
            .LogToTrace();
    }
}
```

#### 1.4 iOS 项目配置

```xml
<!-- ReadStorm.iOS.csproj -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0-ios</TargetFramework>
    <OutputType>Exe</OutputType>
    <RuntimeIdentifier>ios-arm64</RuntimeIdentifier>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="11.3.11" />
    <PackageReference Include="Avalonia.iOS" Version="11.3.11" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="11.3.11" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="11.3.11" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.2.1" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.0" />
    <PackageReference Include="Semi.Avalonia" Version="11.2.1" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ReadStorm.Application\ReadStorm.Application.csproj" />
    <ProjectReference Include="..\ReadStorm.Infrastructure\ReadStorm.Infrastructure.csproj" />
  </ItemGroup>
</Project>
```

#### 1.5 iOS 入口文件

```csharp
// AppDelegate.cs
using Avalonia;
using Avalonia.iOS;
using Foundation;
using UIKit;

namespace ReadStorm.iOS;

[Register("AppDelegate")]
public class AppDelegate : AvaloniaAppDelegate<App>
{
    protected override AppBuilder CustomizeAppBuilder(AppBuilder builder)
    {
        return base.CustomizeAppBuilder(builder)
            .WithInterFont()
            .LogToTrace();
    }
}
```

#### 1.6 共享 App 类重构

将 `App.axaml.cs` 中的平台初始化逻辑改为同时支持桌面和移动端：

```csharp
// 修改 App.axaml.cs — OnFrameworkInitializationCompleted
public override void OnFrameworkInitializationCompleted()
{
    DisableAvaloniaDataAnnotationValidation();
    _serviceProvider = ConfigureServices();

    var mainViewModel = _serviceProvider.GetRequiredService<MainWindowViewModel>();

    if (ApplicationLifetime is IClassicDesktopStyleApplicationLifetime desktop)
    {
        desktop.MainWindow = new MainWindow { DataContext = mainViewModel };
        desktop.Exit += (_, _) => _serviceProvider?.Dispose();
    }
    else if (ApplicationLifetime is ISingleViewApplicationLifetime singleView)
    {
        singleView.MainView = new MainView { DataContext = mainViewModel };
    }

    base.OnFrameworkInitializationCompleted();
}
```

#### 1.7 验收标准

- [ ] Android 模拟器上成功启动并显示主界面
- [ ] iOS 模拟器上成功启动并显示主界面（需 macOS + Xcode）
- [ ] 桌面端功能无回退

---

### 阶段二：UI 响应式适配（5-7 天）

#### 2.1 创建移动端专用主视图

为移动端创建 `MainView`（UserControl），区别于桌面端的 `MainWindow`：

```
src/ReadStorm.Desktop/Views/
├── MainWindow.axaml          # 桌面端（保持不变）
├── MainView.axaml            # 新增：移动端主视图（UserControl）
└── ...
```

**关键差异**：
- 桌面端使用 `Window`，移动端使用 `UserControl`
- 共享同一个 `MainWindowViewModel`（ViewModel 100% 复用）

#### 2.2 布局适配策略

| 桌面端（现有） | 移动端（适配） | 适配方式 |
|---------------|---------------|----------|
| 左侧导航栏（TabControl 竖排） | 底部导航栏（TabStrip） | 替换布局方向 |
| 大面积表格展示搜索结果 | 卡片列表（StackPanel / ItemsRepeater） | 简化列宽，改为纵向滚动 |
| 弹窗对话框 | 全屏页面或底部弹出面板 | 替换 Dialog 为 Panel |
| 鼠标双击操作 | 单击 + 长按菜单 | 添加触控手势 |
| 固定窗口尺寸 | 全屏自适应 | 使用 `StackPanel`/`Grid` 百分比布局 |

#### 2.3 响应式布局实现

使用 Avalonia 的 `AdaptiveTrigger` 或手动判断屏幕宽度来切换布局：

```xml
<!-- 示例：根据屏幕宽度选择布局 -->
<Grid>
    <!-- 窄屏（手机）：底部标签栏 -->
    <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />   <!-- 底部导航 -->
    </Grid.RowDefinitions>

    <ContentControl Grid.Row="0" Content="{Binding CurrentPage}" />

    <!-- 底部导航栏 -->
    <StackPanel Grid.Row="1" Orientation="Horizontal"
                HorizontalAlignment="Center">
        <Button Content="🔍 搜索" Command="{Binding SwitchToSearchCommand}" />
        <Button Content="📚 书架" Command="{Binding SwitchToBookshelfCommand}" />
        <Button Content="⚙️ 设置" Command="{Binding SwitchToSettingsCommand}" />
    </StackPanel>
</Grid>
```

#### 2.4 各页面适配要点

##### 搜索页
- 搜索栏置顶，关键词输入框占满宽度
- 搜索结果改为垂直卡片列表，每个卡片显示：书名、作者、书源
- 点击卡片进入详情 / 下载

##### 书架页
- 网格布局（2-3 列），封面缩略图 + 书名
- 长按弹出操作菜单（删除、导出、查看详情）

##### 阅读器页
- 全屏显示章节内容
- 底部工具栏：上一章 / 目录 / 下一章
- 支持上下滑动翻页（ScrollViewer）
- 点击屏幕中央区域显示/隐藏工具栏

##### 设置页
- 垂直列表布局，每项占一行
- 字体大小可用滑块调节

#### 2.5 验收标准

- [ ] 搜索页在手机竖屏上可正常使用
- [ ] 书架页网格布局显示正确
- [ ] 阅读器全屏阅读体验流畅
- [ ] 设置页所有选项可操作

---

### 阶段三：平台特定适配（3-5 天）

#### 3.1 文件路径适配

移动端的文件系统路径与桌面端不同，需要抽象平台路径：

```csharp
// 新增接口：ReadStorm.Application/Abstractions/IPlatformPathProvider.cs
public interface IPlatformPathProvider
{
    /// <summary>应用数据目录（SQLite 数据库、设置文件）</summary>
    string AppDataPath { get; }

    /// <summary>缓存目录（封面缓存等临时文件）</summary>
    string CachePath { get; }

    /// <summary>用户可见文件目录（导出的 TXT/EPUB）</summary>
    string UserFilesPath { get; }
}
```

各平台实现：

```csharp
// 桌面端实现
public class DesktopPathProvider : IPlatformPathProvider
{
    public string AppDataPath => Path.Combine(
        Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData),
        "ReadStorm");
    public string CachePath => Path.Combine(AppDataPath, "cache");
    public string UserFilesPath => Path.Combine(
        Environment.GetFolderPath(Environment.SpecialFolder.MyDocuments),
        "ReadStorm");
}

// Android 实现
public class AndroidPathProvider : IPlatformPathProvider
{
    public string AppDataPath => Environment.GetFolderPath(
        Environment.SpecialFolder.LocalApplicationData);
    public string CachePath => Path.Combine(AppDataPath, "cache");
    public string UserFilesPath => Path.Combine(AppDataPath, "exports");
}

// iOS 实现
public class IosPathProvider : IPlatformPathProvider
{
    public string AppDataPath => Environment.GetFolderPath(
        Environment.SpecialFolder.LocalApplicationData);
    public string CachePath => Path.Combine(AppDataPath, "Library", "Caches");
    public string UserFilesPath => Path.Combine(AppDataPath, "Documents");
}
```

#### 3.2 书源规则文件加载适配

移动端无法使用桌面端的 `CopyToOutputDirectory` 方式加载规则文件。需改为 **嵌入资源（Embedded Resource）** 方式：

```xml
<!-- ReadStorm.Infrastructure.csproj 中添加 -->
<ItemGroup>
  <EmbeddedResource Include="rules\*.json" />
</ItemGroup>
```

同时修改 `EmbeddedRuleCatalogUseCase` 或 `RuleFileLoader`，优先从嵌入资源加载：

```csharp
// 伪代码：优先从嵌入资源加载，回退到文件系统
var assembly = typeof(EmbeddedRuleCatalogUseCase).Assembly;
var resourceNames = assembly.GetManifestResourceNames()
    .Where(n => n.EndsWith(".json"));

foreach (var name in resourceNames)
{
    using var stream = assembly.GetManifestResourceStream(name);
    // 解析 JSON 规则...
}
```

#### 3.3 网络权限配置

**Android**（`AndroidManifest.xml`）：
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<!-- Android 9+ 明文流量 -->
<application android:usesCleartextTraffic="true" ... />
```

**iOS**（`Info.plist`）：
```xml
<!-- 如需访问 HTTP（非 HTTPS）站点 -->
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoads</key>
    <true/>
</dict>
```

> ⚠️ 许多书源使用 HTTP 而非 HTTPS，因此需要允许明文流量。

#### 3.4 SQLite 路径适配

修改 `SqliteBookRepository` 构造函数，使用 `IPlatformPathProvider` 获取数据库路径：

```csharp
public SqliteBookRepository(IPlatformPathProvider pathProvider)
{
    var dbPath = Path.Combine(pathProvider.AppDataPath, "readstorm.db");
    Directory.CreateDirectory(Path.GetDirectoryName(dbPath)!);
    _connectionString = $"Data Source={dbPath}";
}
```

#### 3.5 验收标准

- [ ] Android 端可正确读写 SQLite 数据库
- [ ] Android 端可加载嵌入的书源规则文件
- [ ] Android 端可正常发起 HTTP 请求
- [ ] iOS 端以上功能同样可用
- [ ] 桌面端无功能回退

---

### 阶段四：阅读器移动端优化（3-4 天）

#### 4.1 触控手势支持

| 手势 | 功能 |
|------|------|
| 左滑 | 下一章 |
| 右滑 | 上一章 |
| 单击屏幕中央 | 显示/隐藏工具栏 |
| 长按文字 | 复制选中文本 |
| 双指缩放 | 调整字体大小 |
| 上下滑动 | 章节内容滚动 |

```csharp
// 在 ReaderView 中添加手势处理
private void OnSwipeLeft(object? sender, SwipeEventArgs e)
{
    if (DataContext is ReaderViewModel vm)
        vm.NextChapterCommand.Execute(null);
}

private void OnSwipeRight(object? sender, SwipeEventArgs e)
{
    if (DataContext is ReaderViewModel vm)
        vm.PreviousChapterCommand.Execute(null);
}
```

#### 4.2 屏幕常亮

阅读时保持屏幕常亮，防止自动息屏：

```csharp
// Android
Window.AddFlags(WindowManagerFlags.KeepScreenOn);

// iOS
UIApplication.SharedApplication.IdleTimerDisabled = true;
```

#### 4.3 阅读进度保存优化

移动端随时可能被系统杀死进程，需在以下时机保存阅读进度：
- 切换章节时
- 应用进入后台时（`OnPause` / `applicationDidEnterBackground`）
- 定时自动保存（每 30 秒）

#### 4.4 验收标准

- [ ] 左右滑动翻章正常工作
- [ ] 阅读时屏幕保持常亮
- [ ] 应用被杀后重新打开可恢复阅读进度
- [ ] 字体大小可通过手势或设置调节

---

### 阶段五：测试 & 调试（3-5 天）

#### 5.1 测试策略

| 测试类型 | 内容 | 工具 |
|----------|------|------|
| 单元测试 | Domain / Application / Infrastructure 业务逻辑 | xUnit（现有 8 个测试不变） |
| UI 测试 | 移动端页面导航、布局正确性 | Avalonia Headless 测试 |
| 集成测试 | 搜索 → 下载 → 阅读全流程 | Android 模拟器手动测试 |
| 兼容性测试 | 不同 Android 版本 / 不同屏幕尺寸 | 多模拟器配置 |

#### 5.2 重点测试场景

1. **搜索功能**：输入关键词 → 选择书源 → 获取搜索结果
2. **下载功能**：选择小说 → 全本下载 → 下载完成通知
3. **阅读功能**：打开书籍 → 翻页 → 切换章节 → 调整设置
4. **离线阅读**：断网 → 打开已下载书籍 → 正常阅读
5. **生命周期**：切后台 → 返回 → 进度恢复
6. **低内存**：系统回收应用 → 重新启动 → 数据不丢失

#### 5.3 已知需关注的问题

- **AngleSharp 在移动端的内存占用**：解析大 HTML 页面时需注意 GC 压力
- **SSL 证书校验**：部分书源站点证书不规范，移动端可能比桌面端更严格
- **并发下载数**：移动端建议限制为 2-3 个并发，避免电量和流量消耗过大

#### 5.4 验收标准

- [ ] 所有现有单元测试通过
- [ ] 搜索 → 下载 → 阅读全流程在 Android 上跑通
- [ ] 搜索 → 下载 → 阅读全流程在 iOS 上跑通
- [ ] 横屏 / 竖屏切换无崩溃

---

### 阶段六：打包发布（2-3 天）

#### 6.1 Android 打包

```bash
# 生成 APK（调试版）
dotnet build src/ReadStorm.Android -c Debug -t:Install

# 生成 AAB（发布版，用于 Google Play）
dotnet publish src/ReadStorm.Android -c Release \
  -p:AndroidKeyStore=true \
  -p:AndroidSigningKeyStore=readstorm.keystore \
  -p:AndroidSigningKeyAlias=readstorm \
  -p:AndroidSigningKeyPass=<password> \
  -p:AndroidSigningStorePass=<password>
```

#### 6.2 iOS 打包

```bash
# 生成 IPA（需 macOS + Xcode + Apple 开发者账号）
dotnet publish src/ReadStorm.iOS -c Release \
  -r ios-arm64 \
  -p:CodesignKey="Apple Distribution: ..." \
  -p:CodesignProvision="..."
```

#### 6.3 CI/CD 配置

在 `.github/workflows/` 中添加移动端构建工作流：

```yaml
# .github/workflows/mobile-build.yml
name: Mobile Build
on: [push, pull_request]
jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - run: dotnet workload install android
      - run: dotnet build src/ReadStorm.Android -c Release

  ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - run: dotnet workload install ios
      - run: dotnet build src/ReadStorm.iOS -c Release
```

#### 6.4 分发渠道

| 渠道 | Android | iOS |
|------|---------|-----|
| 官方商店 | Google Play | App Store |
| 侧载 | APK 直接下载 | TestFlight（需开发者账号） |
| GitHub Release | ✅ 上传 APK | ✅ 上传 IPA |

#### 6.5 验收标准

- [ ] Android APK 可在真机安装运行
- [ ] iOS IPA 可在真机安装运行（通过 TestFlight）
- [ ] CI 可自动构建 Android / iOS

---

## 四、代码改动范围评估

### 4.1 各层改动量

| 层 | 预估改动量 | 说明 |
|----|-----------|------|
| **Domain** | 🟢 零改动 | 纯模型层，无平台依赖 |
| **Application** | 🟡 微量改动 | 新增 `IPlatformPathProvider` 接口（约 10 行） |
| **Infrastructure** | 🟡 少量改动 | 路径获取改用 `IPlatformPathProvider`；规则文件改为嵌入资源读取（约 50 行） |
| **Desktop** | 🟡 少量改动 | `App.axaml.cs` 添加移动端生命周期支持（约 20 行） |
| **新增 Android 项目** | 🔵 新增 | 项目配置 + 入口文件 + 资源文件（约 100 行） |
| **新增 iOS 项目** | 🔵 新增 | 项目配置 + 入口文件 + 资源文件（约 80 行） |
| **UI 视图（共享）** | 🟠 中等改动 | 新增 `MainView.axaml`（移动端布局）；阅读器手势支持（约 300 行） |

**总代码改动量：约 500-600 行新增/修改代码**（不含 XAML 样式微调）

### 4.2 不需要改动的部分

以下组件可完全复用，**无需任何修改**：

- ✅ 所有 ViewModel（`MainWindowViewModel`、`ReaderViewModel`、`BookshelfViewModel` 等 12 个）
- ✅ 所有 Domain 模型（14 个）
- ✅ 所有 Application 接口（10 个）
- ✅ 大部分 Infrastructure 服务（21 个中约 18 个无需改动）
- ✅ 所有书源规则 JSON 文件（22 个）
- ✅ 现有测试（继续通过）

---

## 五、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| Semi.Avalonia 移动端兼容性不足 | 部分主题样式异常 | 移动端回退到 `Avalonia.Themes.Fluent`，或仅用 Semi.Avalonia 兼容的子集 |
| 部分书源 HTTP 被手机系统拦截 | 无法访问某些书源 | 配置明文流量许可；长期考虑代理或 HTTPS 适配 |
| iOS 打包需 macOS + 开发者账号 | 无法本地构建 iOS | 优先保证 Android 端；iOS 使用 GitHub Actions macOS Runner |
| Avalonia 移动端控件触控体验不佳 | 交互不流畅 | 使用 Avalonia 的 `GestureRecognizer`；必要时针对关键控件自定义渲染 |
| 大文件下载时后台被系统杀死 | 下载中断 | 实现断点续传；使用 Android `ForegroundService` / iOS `Background Fetch` |

---

## 六、后续优化方向（非本次范围）

完成基础移动端迁移后，可考虑的后续优化：

1. **云同步**：使用 WebDAV 或自建 API 同步书架和阅读进度
2. **本地通知**：新章节更新提醒（Android Notification / iOS Push）
3. **夜间模式**：跟随系统深色模式自动切换
4. **语音朗读**：集成系统 TTS（Text-to-Speech）引擎
5. **Widget**：Android 桌面小组件显示阅读进度
6. **平板适配**：iPad / Android 平板横屏双栏布局

---

## 七、开发环境准备

### 7.1 必备工具

| 工具 | 用途 | 安装方式 |
|------|------|----------|
| .NET 10 SDK | 编译运行 | https://dotnet.microsoft.com |
| Android SDK | Android 构建 | `dotnet workload install android` |
| iOS SDK | iOS 构建 | `dotnet workload install ios`（仅 macOS） |
| Android 模拟器 | 测试 | Android Studio → AVD Manager |
| Xcode | iOS 构建 & 模拟器 | Mac App Store（仅 macOS） |
| JDK 17+ | Android 构建依赖 | https://adoptium.net |

### 7.2 快速验证命令

```bash
# 安装 Android 工作负载
dotnet workload install android

# 构建 Android 项目
dotnet build src/ReadStorm.Android

# 部署到 Android 模拟器
dotnet build src/ReadStorm.Android -t:Install -f net10.0-android

# 安装 iOS 工作负载（仅 macOS）
dotnet workload install ios

# 构建 iOS 项目
dotnet build src/ReadStorm.iOS
```

---

## 八、总结

| 维度 | 评估 |
|------|------|
| **技术可行性** | ✅ 高 — Avalonia 原生支持 Android/iOS，核心依赖全部兼容 |
| **改造成本** | 🟡 中等偏低 — 业务层零改动，主要是 UI 适配和平台配置 |
| **代码复用率** | 📊 约 85-90% — Domain / Application / Infrastructure / ViewModels 全部复用 |
| **预估工期** | 📅 3.5-5.5 周（1 人全职） |
| **推荐路线** | 🏆 Avalonia 移动端扩展（方案 A），最小改动成本 |
| **优先平台** | 📱 先做 Android（工具链更成熟，无需 macOS），再做 iOS |
