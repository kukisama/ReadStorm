# ReadStorm Android 实施方案（方案 A：Avalonia 移动端扩展）

> 基于方案对比分析，确认采用"同解决方案新增 Android 项目"（方案 1）实施 Android 端开发。本文档详细说明如何最大限度复用现有代码来构建 Android 应用。

---

## 一、复用策略总览

### 1.1 各层复用情况

```
┌─────────────────────────────────────────────────────┐
│                   复用层级图                          │
├──────────────────┬──────────────┬────────────────────┤
│       层         │   复用方式    │    改动量          │
├──────────────────┼──────────────┼────────────────────┤
│ Domain           │ 100% 直接引用 │ 零改动             │
│ Application      │ 100% 直接引用 │ 零改动 ~ 微量新增   │
│ Infrastructure   │ ~95% 直接引用 │ 微量适配（路径）    │
│ ViewModel        │ 100% 直接引用 │ 零改动             │
│ XAML View        │ 部分复用      │ 新增移动端布局      │
│ Android 启动入口  │ 全新          │ ~100 行            │
└──────────────────┴──────────────┴────────────────────┘
```

### 1.2 新增项目结构

```
src/ReadStorm.Android/
├── ReadStorm.Android.csproj      # 项目配置（TFM: net10.0-android）
├── MainActivity.cs               # Android 入口 Activity
├── MainView.axaml                # 移动端主视图（UserControl）
├── MainView.axaml.cs
├── AndroidManifest.xml           # Android 权限和应用声明
├── Resources/
│   ├── drawable/
│   │   └── icon.png              # 应用图标
│   ├── values/
│   │   └── styles.xml            # Android 主题样式
│   └── mipmap-*/                 # 不同分辨率图标
└── Assets/                       # 可选：移动端专用资源
```

---

## 二、项目配置

### 2.1 Android 项目文件（ReadStorm.Android.csproj）

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0-android</TargetFramework>
    <OutputType>Exe</OutputType>
    <ApplicationId>com.readstorm.app</ApplicationId>
    <ApplicationVersion>1</ApplicationVersion>
    <ApplicationDisplayVersion>1.0.0</ApplicationDisplayVersion>
    <SupportedOSPlatformVersion>21</SupportedOSPlatformVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!-- Avalonia + 业务依赖 -->
  <ItemGroup>
    <PackageReference Include="Avalonia" Version="11.3.11" />
    <PackageReference Include="Avalonia.Android" Version="11.3.11" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="11.3.11" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="11.3.11" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.2.1" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.0" />
    <PackageReference Include="Semi.Avalonia" Version="11.2.1" />
  </ItemGroup>

  <!-- 引用核心业务层 — 这就是"复用"的关键 -->
  <ItemGroup>
    <ProjectReference Include="..\ReadStorm.Application\ReadStorm.Application.csproj" />
    <ProjectReference Include="..\ReadStorm.Infrastructure\ReadStorm.Infrastructure.csproj" />
  </ItemGroup>
</Project>
```

**说明**：
- `TargetFramework` 为 `net10.0-android`，这是 .NET 10 对 Android 的官方支持
- `SupportedOSPlatformVersion` 设为 21（Android 5.0），覆盖绝大多数设备
- 通过 `ProjectReference` 直接引用 `Application` 和 `Infrastructure` 层，间接引用 `Domain` 层
- 所有 NuGet 包版本与桌面端保持一致，确保运行时行为一致

### 2.2 解决方案文件更新

在 `ReadStorm.slnx` 中添加 Android 项目：

```xml
<Solution>
  <Folder Name="/src/">
    <Project Path="src/ReadStorm.Application/ReadStorm.Application.csproj" />
    <Project Path="src/ReadStorm.Desktop/ReadStorm.Desktop.csproj" />
    <Project Path="src/ReadStorm.Domain/ReadStorm.Domain.csproj" />
    <Project Path="src/ReadStorm.Infrastructure/ReadStorm.Infrastructure.csproj" />
    <Project Path="src/ReadStorm.Android/ReadStorm.Android.csproj" />  <!-- 新增 -->
  </Folder>
  <Folder Name="/tests/">
    <Project Path="tests/ReadStorm.Tests/ReadStorm.Tests.csproj" />
  </Folder>
</Solution>
```

---

## 三、核心代码复用

### 3.1 App 类共享

桌面端的 `App.axaml.cs` 需添加移动端生命周期支持。关键修改仅一处——在 `OnFrameworkInitializationCompleted` 中增加 `ISingleViewApplicationLifetime` 分支：

```csharp
public override void OnFrameworkInitializationCompleted()
{
    DisableAvaloniaDataAnnotationValidation();
    _serviceProvider = ConfigureServices();

    var mainViewModel = _serviceProvider.GetRequiredService<MainWindowViewModel>();

    if (ApplicationLifetime is IClassicDesktopStyleApplicationLifetime desktop)
    {
        // 桌面端 — 使用 Window（现有逻辑不变）
        desktop.MainWindow = new MainWindow { DataContext = mainViewModel };
        desktop.Exit += (_, _) => _serviceProvider?.Dispose();
    }
    else if (ApplicationLifetime is ISingleViewApplicationLifetime singleView)
    {
        // 移动端 — 使用 UserControl
        singleView.MainView = new MainView { DataContext = mainViewModel };
    }

    base.OnFrameworkInitializationCompleted();
}
```

**改动量**：约 5 行新增代码。桌面端逻辑完全不变。

### 3.2 Android 入口（MainActivity.cs）

```csharp
using Android.Content.PM;
using Avalonia;
using Avalonia.Android;

namespace ReadStorm.Android;

[Activity(
    Label = "ReadStorm",
    Theme = "@style/MyTheme.NoActionBar",
    Icon = "@drawable/icon",
    MainLauncher = true,
    ConfigurationChanges = ConfigChanges.Orientation
        | ConfigChanges.ScreenSize
        | ConfigChanges.UiMode)]
public class MainActivity : AvaloniaMainActivity<App>
{
    protected override AppBuilder CustomizeAppBuilder(AppBuilder builder)
    {
        return base.CustomizeAppBuilder(builder)
            .WithInterFont()
            .LogToTrace();
    }
}
```

**说明**：`AvaloniaMainActivity<App>` 中的 `App` 就是现有桌面端的 `App` 类，Avalonia 框架会自动根据平台选择 `IClassicDesktopStyleApplicationLifetime`（桌面）或 `ISingleViewApplicationLifetime`（移动端）。

### 3.3 ViewModel 层 — 零改动

所有 ViewModel 均通过接口依赖业务层，不包含任何平台特定代码：

- `MainWindowViewModel` — 页签导航逻辑
- `SearchDownloadViewModel` — 搜索和下载管理
- `BookshelfViewModel` — 书架管理
- `ReaderViewModel` — 阅读器逻辑
- `DiagnosticViewModel` — 书源诊断
- `RuleEditorViewModel` — 书源编辑
- `SettingsViewModel` — 设置管理

这些 ViewModel **全部直接复用**，无需任何修改。

### 3.4 业务层 — 零改动

- `Domain` 层：纯 C# 模型和枚举，无平台依赖
- `Application` 层：纯接口定义，无平台依赖
- `Infrastructure` 层：
  - `HybridSearchBooksUseCase` — HTTP + AngleSharp，跨平台兼容 ✅
  - `RuleBasedDownloadBookUseCase` — 下载引擎，跨平台兼容 ✅
  - `SqliteBookRepository` — SQLite 读写，跨平台兼容 ✅
  - `EmbeddedRuleCatalogUseCase` — 嵌入资源读取，跨平台兼容 ✅
  - `EpubExporter` — 文件导出，需注意路径适配 ⚠️

---

## 四、需要适配的部分

### 4.1 移动端主视图（MainView）

桌面端使用 `MainWindow`（Window 类型），移动端需使用 `MainView`（UserControl 类型）。这是唯一的结构性差异。

```xml
<!-- MainView.axaml — 移动端专用 -->
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="ReadStorm.Desktop.Views.MainView">

    <Grid RowDefinitions="*, Auto">
        <!-- 内容区域 -->
        <ContentControl Grid.Row="0"
                        Content="{Binding CurrentPageViewModel}" />

        <!-- 底部导航栏 -->
        <Border Grid.Row="1" Background="{DynamicResource SemiColorBg1}">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Spacing="24" Margin="0,8">
                <Button Content="🔍 搜索"
                        Command="{Binding SwitchToSearchCommand}" />
                <Button Content="📚 书架"
                        Command="{Binding SwitchToBookshelfCommand}" />
                <Button Content="📖 阅读"
                        Command="{Binding SwitchToReaderCommand}" />
                <Button Content="⚙️ 设置"
                        Command="{Binding SwitchToSettingsCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
```

### 4.2 文件路径适配

Android 文件系统与桌面端不同，可通过条件编译或平台服务抽象来处理：

```csharp
// 简单方案：条件编译
public static string GetAppDataPath()
{
#if ANDROID
    return Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData);
#else
    return Path.Combine(
        Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData),
        "ReadStorm");
#endif
}
```

或者通过接口抽象（更优雅）：

```csharp
// Application 层新增接口
public interface IPlatformPathProvider
{
    string AppDataPath { get; }
    string CachePath { get; }
    string UserFilesPath { get; }
}
```

### 4.3 Android 权限配置

`AndroidManifest.xml` 需声明网络和存储权限：

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:label="ReadStorm"
        android:icon="@drawable/icon"
        android:usesCleartextTraffic="true"
        android:theme="@style/MyTheme.NoActionBar">
    </application>
</manifest>
```

> ⚠️ `usesCleartextTraffic="true"` 是必要的，因为许多书源网站使用 HTTP 而非 HTTPS。

---

## 五、复用代码流程图

```
┌───────────────────────────────────────────────────┐
│                Android 应用启动流程                 │
└───────────────────────────────────────────────────┘

 Android OS
    │
    ▼
 MainActivity.cs  ← 新增（仅 Android）
    │
    ▼
 App.axaml.cs     ← 现有（微量修改：添加移动端分支）
    │
    ├── ISingleViewApplicationLifetime  → MainView     ← 新增（移动端布局）
    │
    ▼
 ConfigureServices()  ← 现有（零改动）
    │
    ├── MainWindowViewModel      ← 100% 复用
    ├── ISearchBooksUseCase      ← 100% 复用
    ├── IDownloadBookUseCase     ← 100% 复用
    ├── IBookRepository          ← 100% 复用
    ├── IRuleCatalogUseCase      ← 100% 复用
    └── ...其他服务               ← 100% 复用
```

---

## 六、开发环境要求

### 6.1 额外安装

| 工具 | 安装方式 | 说明 |
|------|----------|------|
| Android workload | `dotnet workload install android` | .NET Android 编译支持 |
| JDK 17+ | 系统安装或通过 Android Studio | Android 构建依赖 |
| Android SDK | 通过 Android Studio 或命令行 | 编译和模拟器 |
| Android 模拟器 | Android Studio → AVD Manager | 测试运行 |

### 6.2 构建命令

```bash
# 仅构建 Android 项目
dotnet build src/ReadStorm.Android

# 构建并部署到模拟器
dotnet build src/ReadStorm.Android -t:Install -f net10.0-android

# 构建桌面端（不受影响）
dotnet build src/ReadStorm.Desktop

# 构建全部（需已安装 Android workload）
dotnet build ReadStorm.slnx
```

---

## 七、阶段性目标

### 第一阶段：最小可运行版（2-3 天）

- [ ] 创建 `ReadStorm.Android` 项目
- [ ] 配置 `AndroidManifest.xml` 和权限
- [ ] 编写 `MainActivity.cs` 入口
- [ ] 修改 `App.axaml.cs` 支持移动端生命周期
- [ ] 创建简易 `MainView.axaml`
- [ ] 在 Android 模拟器上成功启动

**此阶段目标**：应用能在 Android 模拟器上启动并显示界面，搜索功能基本可用。

### 第二阶段：UI 移动端适配（5-7 天）

- [ ] 搜索页适配为竖屏卡片列表
- [ ] 书架页适配为网格布局
- [ ] 阅读器页全屏展示 + 底部工具栏
- [ ] 设置页竖向列表化
- [ ] 底部导航栏交互优化

### 第三阶段：平台与体验优化（3-5 天）

- [ ] 文件路径平台适配
- [ ] 触控手势（滑动翻页）
- [ ] 屏幕常亮
- [ ] 阅读进度自动保存
- [ ] APK 打包与签名

---

## 八、总结

采用方案 1（同解决方案新增 Android 项目）的核心优势在于：

| 指标 | 数值 |
|------|------|
| 代码复用率 | ~90%（Domain/Application/Infrastructure/ViewModel 全复用） |
| 新增代码量 | ~100 行启动代码 + ~300 行 UI 适配 |
| 核心层改动 | 0-50 行 |
| 首版可用时间 | 2-3 天 |
| 完整版时间 | 3-5 周 |
| 桌面端影响 | 零风险 |

**这是成本最低、风险最小、复用最高的实施路径。**
