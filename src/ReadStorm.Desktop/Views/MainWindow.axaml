<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:ReadStorm.Desktop.ViewModels"
        xmlns:views="using:ReadStorm.Desktop.Views"
    xmlns:md="clr-namespace:Markdown.Avalonia;assembly=Markdown.Avalonia"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="1080" d:DesignHeight="720"
        x:Class="ReadStorm.Desktop.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/app.ico"
    Title="{Binding Title}"
    MinWidth="{Binding WindowMinWidth}" MinHeight="540"
    Background="#F0F1F3">

    <Grid RowDefinitions="Auto,*">
        <!-- top status bar -->
        <Border Grid.Row="0" Background="#1E293B" Padding="14,10">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
                    <Border Width="32" Height="32" CornerRadius="6" Background="#3B82F6">
                        <TextBlock Text="RS" FontSize="14" FontWeight="Bold" Foreground="White"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="阅读风暴" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" Foreground="#94A3B8"
                           VerticalAlignment="Center" Margin="20,0,0,0" FontSize="13" TextTrimming="CharacterEllipsis"
                           ToolTip.Tip="{Binding StatusMessage}" />
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Border Background="#334155" CornerRadius="12" Padding="10,4"
                            IsVisible="{Binding SearchDownload.ActiveDownloadSummary, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                        <TextBlock Text="{Binding SearchDownload.ActiveDownloadSummary}"
                                   Foreground="#60A5FA" FontSize="12" />
                    </Border>
                    <Border Background="#334155" CornerRadius="12" Padding="10,4">
                        <TextBlock Text="{Binding AvailableSourceCount, StringFormat=书源 {0}}"
                                   Foreground="#94A3B8" FontSize="12"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- sidebar nav + content -->
        <TabControl Grid.Row="1" TabStripPlacement="Left" SelectedIndex="{Binding SelectedTabIndex}">

            <!-- search tab -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="8" Height="8" CornerRadius="4" Background="#3B82F6" VerticalAlignment="Center"/>
                        <TextBlock Text="搜索下载" FontSize="13"/>
                    </StackPanel>
                </TabItem.Header>
                <views:SearchView DataContext="{Binding SearchDownload}" />
            </TabItem>

            <!-- download tasks tab -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="8" Height="8" CornerRadius="4" Background="#8B5CF6" VerticalAlignment="Center"/>
                        <TextBlock Text="下载任务" FontSize="13"/>
                    </StackPanel>
                </TabItem.Header>
                <views:DownloadTaskView DataContext="{Binding SearchDownload}" />
            </TabItem>

            <!-- diagnostic tab -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="8" Height="8" CornerRadius="4" Background="#F59E0B" VerticalAlignment="Center"/>
                        <TextBlock Text="书源诊断" FontSize="13"/>
                    </StackPanel>
                </TabItem.Header>
                <views:DiagnosticView DataContext="{Binding Diagnostic}" />
            </TabItem>

            <!-- bookshelf tab -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="8" Height="8" CornerRadius="4" Background="#10B981" VerticalAlignment="Center"/>
                        <TextBlock Text="书架" FontSize="13"/>
                    </StackPanel>
                </TabItem.Header>
                <views:BookshelfView DataContext="{Binding Bookshelf}" />
            </TabItem>

            <!-- reader tab -->
            <TabItem IsVisible="{Binding IsReaderTabVisible}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8"
                                ToolTip.Tip="请先在书架中打开一本书">
                        <Border Width="8" Height="8" CornerRadius="4" Background="#EC4899" VerticalAlignment="Center"/>
                        <TextBlock Text="阅读" FontSize="13"/>
                    </StackPanel>
                </TabItem.Header>
                <views:ReaderView DataContext="{Binding Reader}" />
            </TabItem>

            <!-- rule editor tab -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="8" Height="8" CornerRadius="4" Background="#6366F1" VerticalAlignment="Center"/>
                        <TextBlock Text="规则处理" FontSize="13"/>
                    </StackPanel>
                </TabItem.Header>
                <views:RuleEditorView DataContext="{Binding RuleEditor}" />
            </TabItem>

            <!-- settings tab -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="8" Height="8" CornerRadius="4" Background="#6B7280" VerticalAlignment="Center"/>
                        <TextBlock Text="设置" FontSize="13"/>
                    </StackPanel>
                </TabItem.Header>
                <views:SettingsView DataContext="{Binding Settings}" />
            </TabItem>

            <!-- about tab -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="8" Height="8" CornerRadius="4" Background="#0EA5E9" VerticalAlignment="Center"/>
                        <TextBlock Text="关于" FontSize="13"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer DataContext="{Binding Settings}">
                    <StackPanel Margin="16" Spacing="10" MaxWidth="980">
                        <TextBlock Text="ReadStorm" FontWeight="Bold" FontSize="22"/>
                        <TextBlock Text="{Binding AboutVersion, StringFormat=v{0}}"
                                   FontSize="13" Foreground="#64748B"/>
                        <Button HorizontalAlignment="Left"
                                Command="{Binding OpenProjectRepositoryCommand}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="🔗" FontSize="14" VerticalAlignment="Center"/>
                                <TextBlock Text="GitHub 项目主页" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8" Padding="12" Background="White">
                            <md:MarkdownScrollViewer Markdown="{Binding AboutContent}"
                                                     HorizontalAlignment="Stretch"
                                                     VerticalAlignment="Stretch"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>

</Window>
